# 🎵 Playlist Installer

Public YouTube playlist installer for automation.

This tool downloads all songs from a **public YouTube playlist**, converts them to **MP3 format**, and saves them into a local `musics/` folder.

---

## 🚀 Features

- 📥 Download entire public YouTube playlists
- 🎧 Automatically convert videos to MP3
- 📂 Save files into a `musics/` directory
- ⚡ Simple URL-based usage
- 🐍 Built with Python

---

## 🧰 Requirements

You must have the following installed:

- Python 3.8+
- `yt-dlp` library
- `ffmpeg`

---

## 📦 Installation

### 1️⃣ Install Python (if not installed)

Linux:
```bash
sudo apt install python3
